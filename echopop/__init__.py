"""
Echopop — acoustic-trawl survey population estimation toolkit.

This is the top-level package. It exposes the package version and serves as the import root for
all sub-packages (``core``, ``ingest``, ``survey``, ``geostatistics``, ``inversion``, ``graphics``,
``validators``, and ``workflows``).
"""

from ._version import version as __version__  # noqa
